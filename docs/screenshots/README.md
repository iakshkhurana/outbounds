# Screenshots

Drop product screenshots here for the root README:

| File | Shot |
|---|---|
| `overview.png` | Home metrics + live feed |
| `host-detail.png` | Host page with risk reasons |
| `report.png` | Report export view |

Tips:

1. Run API + web (or `docker compose up --build`)
2. Click **Replay sample**
3. Capture at ~1440px wide, dark UI, no unrelated desktop chrome

Then link them from the root `README.md` Images section.
